import type {MenuCategory as Category} from "@/data/menu";import {MenuImageRibbon} from "@/components/MenuImageRibbon";
export function MenuCategory({category}:{category:Category}){const id=category.name.toLowerCase().replace(/[^a-z0-9]+/g,"-");return <section id={id} className="scroll-mt-28 border-t border-navy/15 py-12"><div className="grid gap-8 md:grid-cols-[1fr_2fr]"><div><h2 className="font-serif text-4xl text-navy">{category.name}</h2>{category.note&&<p className="mt-3 max-w-xs text-xs uppercase leading-5 tracking-[.12em] text-gold">{category.note}</p>}</div><div><MenuImageRibbon category={category.name}/><div className="grid gap-x-10 md:grid-cols-2">{category.items.map((item,index)=><article className="border-b border-navy/10 py-5 first:pt-0" key={`${item.name}-${index}`}><div className="flex flex-col gap-2 sm:flex-row sm:justify-between sm:gap-4"><h3 className="font-serif text-2xl text-ink">{item.name}</h3><span className="shrink-0 text-sm font-semibold leading-6 text-navy sm:max-w-[190px] sm:text-right">{item.price}</span></div>{item.description&&<p className="mt-2 text-sm leading-6 text-ink/60">{item.description}</p>}</article>)}</div></div></div></section>}
