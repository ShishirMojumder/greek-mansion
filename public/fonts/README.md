# Brand font files

Place the licensed webfont files in this directory using these exact names:

- `zuume-rough-bold.woff2`
- `norwester.woff2`

The site is already configured to load them. Until the files are supplied, headings fall back to Impact/Arial Narrow and body copy still renders in bundled Poppins.
